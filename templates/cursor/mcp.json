{
  "_comment": "Cursor MCP integration for CTDF Vector Memory.",
  "_comment2": "Copy this file to your project's .cursor/mcp.json directory.",
  "_comment3": "Replace ${CTDF_SCRIPTS} with the actual path to CTDF scripts/.",
  "mcpServers": {
    "ctdf-vector-memory": {
      "command": "python3",
      "args": [
        "${CTDF_SCRIPTS}/mcp_server.py",
        "--root", "."
      ],
      "env": {
        "CTDF_PROJECT_ROOT": "."
      }
    }
  }
}
