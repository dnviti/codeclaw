{
  "_comment": "Cursor MCP integration for CodeClaw Vector Memory.",
  "_comment2": "Copy this file to your project's .cursor/mcp.json directory.",
  "_comment3": "Replace ${CLAW_SCRIPTS} with the actual path to CodeClaw scripts/.",
  "mcpServers": {
    "claw-vector-memory": {
      "command": "python3",
      "args": [
        "${CLAW_SCRIPTS}/mcp_server.py",
        "--root", "."
      ],
      "env": {
        "CLAW_PROJECT_ROOT": "."
      }
    }
  }
}
